# Server Configuration
server.port=8080 

# CORS/Management Configuration (Existing)
management.endpoints.web.cors.allowed-origins=*
management.endpoints.web.cors.allowed-methods=*

# ----------------------------------------------------------------------------------
# MongoDB Configuration (Using MongoDB Atlas - Recommended for cheap scaling)
# ----------------------------------------------------------------------------------

# FIX: The URI MUST include the database name ('/jklub_db') AND the connection options.
# Assuming your database name is 'jklub_db'. If not, replace it.

spring.data.mongodb.uri=mongodb+srv://jeevankaushalkitsguntur_db_user:Jeevan1234@cluster0.vkfhqjz.mongodb.net/jklub_db?retryWrites=true&w=majority

# ----------------------------------------------------------------------------------
# JWT Secret Key (Required by JwtUtils.java)
# ----------------------------------------------------------------------------------

# This is a critical security setting. Using the base64 value from earlier for consistency.
# Note: In a production environment, this should be longer and stored securely.
jwt.secret=NDBENkMzNTI2NjU1NkE1ODZlMzI3MjM1NzUzODc4MkY0MTNGNDQyODQ3MkI0QjYyNTA2NDUzNzc2Nzc4MjE0MQ==

# ----------------------------------------------------------------------------------
# Email Service Configuration (Required by EmailService.java)
# ----------------------------------------------------------------------------------

spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.ssl.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

razorpay.key.id=YOUR_KEY_ID
razorpay.key.secret=YOUR_KEY_SECRET